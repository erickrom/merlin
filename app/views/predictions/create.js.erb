//Append prediction to list of predictions
$("#edit_prediction_match_<%= @prediction.match.id %>").remove();
$('#cancel_prediction_<%= @prediction.match.id %>').remove();
$('#edit_prediction_<%= @prediction.match.id %>').show();
$('#edit_prediction_<%= @prediction.match.id %>').text('Edit Prediction');

$('#my_prediction_match_<%= @prediction.match.id %>').remove();

$('#match_<%= @prediction.match.id %>_predictions').prepend('<%= j render partial: 'prediction', object: @prediction %>');
