<% provide(:page_title, 'All Users') %>

<h1>All Users</h1>

<ul class="users">
  <% @all_users.each do |user| %>
    <li><%="#{user.first_name} #{user.last_name} (#{user.email})"%></li>
  <% end %>
</ul>
